# app/Factory

Construction helpers — for when building a `Model`/`Logic`/`Dictionary` instance is involved enough (multiple sources, conditional shape, etc.) to be worth pulling out of the constructor. Not required for anything simple enough to just `new` directly.
