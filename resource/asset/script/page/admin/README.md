# resource/asset/script/page/admin

Per-page admin JS entry scripts, auto-loaded by `resource/asset/script/admin.js` via `window.INDEX` (see `AssetFacade::index()`) — mirrors `page/public/`. Empty until there's an admin page to back.
