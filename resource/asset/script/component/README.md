# resource/asset/script/component

Shared/reusable JS behavior, included by page scripts — the JS counterpart to `resource/asset/style/component/`. Empty until a second page actually needs to share something with `page/public/home.js`.
